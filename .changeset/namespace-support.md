---
"jest-styled-components": minor
---

Add `namespace` option to `toHaveStyleRule` for matching rules generated with `StyleSheetManager namespace`. Can be set per-assertion or globally via `setStyleRuleOptions({ namespace: '#app' })`.
