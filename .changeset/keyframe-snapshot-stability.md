---
"jest-styled-components": minor
---

Stabilize keyframe names in snapshots by replacing hashed `@keyframes` identifiers with deterministic placeholders (`k0`, `k1`, etc.), matching the existing `c0`/`c1` behavior for class names
